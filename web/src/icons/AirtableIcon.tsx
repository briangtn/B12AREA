import React from 'react';

const airtablePath = (
        <img src="img/airtable_logo.svg" width={24} height={24} alt="airtable" />
);

const AirtableIcon = airtablePath;

export default AirtableIcon;
