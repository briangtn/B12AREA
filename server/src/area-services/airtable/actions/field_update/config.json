{
  "displayName": "Field updated",
  "description": "This action is triggered when a specific field is updated",
  "configSchema": [
    {
      "name": "apiKey",
      "description": "The User APIKEY",
      "type": "string",
      "required": true
    },
    {
      "name": "baseId",
      "description": "The ID of the base into which insert the data",
      "type": "string",
      "required": true
    },
    {
      "name": "tableId",
      "description": "The table of the base into which you want to insert entry",
      "type": "string",
      "required": true
    }
  ]
}
