{
  "displayName": "On new dm",
  "description": "Triggered when the user receive a new Direct Message.",
  "configSchema": [
    {
      "name": "from",
      "description": "The @from of the sender",
      "type": "string",
      "required": false
    },
    {
      "name": "mustMatch",
      "description": "The message must match the given regex to be counted as a trigger, using this variable enable the Matches[i] placeholder to be used",
      "type": "string",
      "required": false
    }
  ],
  "placeholders": [
  ]
}
